version: '3.8'

services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: worldcup_predictions_app
    restart: always
    ports:
      - "3000:3000"
    environment:
      - NODE_ENV=production
      - JWT_SECRET=worldcup_secret_key_2026_docker_container_platform
    volumes:
      # Persists only runtime SQLite database files and backup snapshots cleanly
      - app_data:/app/data

volumes:
  app_data:
